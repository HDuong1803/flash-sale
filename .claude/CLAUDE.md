# Local Claude Entry

Canonical project context is defined in `../CLAUDE.md`.

When `.claude/` workflows or agents need project context, use `../CLAUDE.md` as the source of truth and keep this file minimal to avoid drift.
